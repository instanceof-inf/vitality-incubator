hazelcast:
  network:
    join:
      multicast:
        enabled: true
  map:
    default:
      time-to-live-seconds: 5
      max-idle-seconds: 5
      eviction:
        eviction-policy: LRU
        max-size-policy: PER_NODE
        size: 5000